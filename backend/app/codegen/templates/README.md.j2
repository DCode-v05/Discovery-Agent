# {{ system_name }} Connector

_Generated by the Discovery Agent for integration: **{{ integration_name }}**._

{{ description }}

## Install

```bash
pip install -r requirements.txt
```

## Configure

Set these environment variables:

- `{{ base_url_env }}` — the {{ system_name }} API base URL
{% if auth.type == "oauth2_client_credentials" %}
- `{{ env_prefix }}_CLIENT_ID` — OAuth client id
- `{{ env_prefix }}_CLIENT_SECRET` — OAuth client secret
- `{{ env_prefix }}_TOKEN_URL` — OAuth token endpoint (defaults to `{{ auth.token_url }}`)
{% elif auth.type == "basic" %}
- `{{ env_prefix }}_USERNAME` — API username
- `{{ env_prefix }}_PASSWORD` — API password
{% else %}
- `{{ env_prefix }}_API_KEY` — API key / token
{% endif %}

## Usage

```python
from connector import {{ class_name }}

client = {{ class_name }}()
{% if entities %}
for item in client.list_{{ entities[0].method }}():
    print(item)
{% endif %}
```

## Test

```bash
pytest -q
```

## Files

- `connector.py` — the connector (auth, CRUD, pagination, rate limiting, retries, errors)
- `test_connector.py` — unit tests (auth, CRUD, pagination, errors, retry)
- `agent.yaml` — agent definition (system prompt, tools, workflow, test scenarios)
- `requirements.txt` — dependencies

> ⚠️ This code is **generated** and ~70–80% production-ready. Review the entity
> resource paths and field names against the live {{ system_name }} API before deploying.
